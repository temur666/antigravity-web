# Mobile Sidebar Swipe Gesture

在移动端添加了触摸滑动手势来切换侧边栏的显示/隐藏。

- **打开**：从屏幕左侧 30px 边缘区域起始，向右滑动超过 50px
- **关闭**：侧边栏打开状态下，向左滑动超过 50px
- 仅水平位移大于垂直位移时触发，避免与正常页面滚动冲突
- 仅在移动端（`<= 768px`）生效

修改文件：`frontend/src/App.tsx`
