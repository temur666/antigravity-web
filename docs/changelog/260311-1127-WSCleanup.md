# 260311-1127 — WS Cleanup (Phase 3)

## 概述

彻底删除 WebSocket 相关代码（阶段三清理）。

## 删除内容

### 后端 (`main.js`)

- `require('ws')` 移除
- `const WebSocket = require('ws')` 移除
- `const wss = new WebSocket.Server(...)` 移除
- `const clients = new Set()` 移除
- 整个 `handleMessage(clientWs, data)` 函数（350 行）删除
  - 所有 `req_*` WS 消息处理逻辑（已全部迁移至 REST 端点）
- `wss.on('connection', ...)` 整个 WS 连接处理块删除
- `ls_disconnected` / `ls_reconnected` 事件中的 WS 广播（for clients）移除
- `broadcastYolo` 中的 WS 广播分支移除
- 启动日志 `[*] WS: ...` 改为 `[*] SSE: ...`

### 前端

- `frontend/src/store/ws-client.ts` — 整个文件删除（379 行）
- `frontend/src/store/__tests__/ws-client.test.ts` — 整个文件删除
- `frontend/src/store/index.ts` — 移除 WSClient/WSClientState 导出

### 测试重写

- `frontend/src/store/__tests__/app-store.test.ts` 全面重写：
  - Mock WSClient + sendAndWait → Mock SSEClient + vi.mock api-client
  - 使用 `vi.hoisted` 解决 vitest mock hoisting 问题
  - 保留所有 23 个原有测试用例语义
  - 新增：sendMessage 被 RUNNING 状态拦截的测试

## 质量检查

- TypeScript: 零错误
- ESLint: 零 warning
- Vitest: 23/23 通过
- PM2 重启: online
